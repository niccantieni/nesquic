global:
    scrape_interval: 10s # Frequency of metric scraping

scrape_configs:
    - job_name: "nesquic"
      honor_timestamps: true
      static_configs:
          - targets: ["pushgateway:9091"]
